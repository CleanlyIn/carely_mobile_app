import 'package:cleanly/pages/model/all_serviceModel.dart';

class ServiceArguments {
  final int index; // Index of the service
  final AllServiceModel service; // The selected service model

  ServiceArguments(this.index, this.service);
}
